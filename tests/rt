TESTSDIR=/home/anton/kvp-server-1.0/tests
EXIT_CODE=0
for TEST in `ls ${TESTSDIR}/?t_*`; do
	cd $TEST
        $TEST
        if [ $? -ne 0 ]; then
                EXIT_CODE=$((EXIT_CODE+1))
        fi
	cd ..
done

exit $EXIT_CODE
